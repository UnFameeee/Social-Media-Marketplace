const auth = {
  login: '/login',
  register: '/register',
};

const friend = {
  home: '/friends',
  request: '/friends/requests',
  sent: '/friends/sent',
  suggestion: '/friends/suggestions',
  all: '/friends/all',
};

const marketplace = {
  home: '/marketplace',
  shopping: '/marketplace/shopping',
  selling: '/marketplace/selling',
};

const flat = {
  profile: '/profile',
  search: '/search',
  postView: '/post',
};
