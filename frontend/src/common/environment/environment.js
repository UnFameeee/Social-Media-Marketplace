export const apiUrl = 'http://localhost:4321/api';

const api = {
  auth: apiUrl + '/auth',
  profile: apiUrl + '/profile',
  friend: apiUrl + '/friendship',
  post: apiUrl + '/post',
  image: apiUrl + '/image',
};

export default api;
